language: objective-c
osx_image: xcode7.3
script: 
  - xctool -workspace Fiery.xcworkspace -scheme FieryTest -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO

branches:
  only:
    - master
    - devel
    - feature/testing
